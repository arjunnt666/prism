# Architecture

collect -> parse -> features -> store
                         \
                          -> diff / analyze

Parsers are expected to lag real SERP markup. That is documented, not hidden.
