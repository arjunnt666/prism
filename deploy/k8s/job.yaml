apiVersion: batch/v1
kind: Job
metadata:
  name: prism-sample-capture
spec:
  template:
    spec:
      containers:
        - name: prism
          image: prism:local
          command: ["prism", "capture", "--query", "example", "--provider", "sample"]
      restartPolicy: Never
  backoffLimit: 1
